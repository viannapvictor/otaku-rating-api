apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.graylog_opensearch.name}}-svc"
spec:
  selector:
    app: {{ .Values.graylog_opensearch.name }}
  type: {{.Values.graylog_opensearch.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.graylog_opensearch.service.type "NodePort")}}
      nodePort: {{.Values.graylog_opensearch.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.graylog_opensearch.service.targetPort}}
      port: {{.Values.graylog_opensearch.service.port}}
