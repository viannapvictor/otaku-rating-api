apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Values.anime_db.name }}-deploy"
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.anime_db.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.anime_db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.anime_db.name }}
    spec:
      containers:
        - name: {{ .Values.anime_db.name }}
          image: "{{ .Values.anime_db.image }}:{{ .Values.anime_db.tag }}"
          imagePullPolicy: {{ .Values.anime_db.imagePullPolicy }}
      restartPolicy: Always
