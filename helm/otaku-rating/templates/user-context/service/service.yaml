apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.user_service.name}}-svc"
spec:
  selector:
    app: {{ .Values.user_service.name }}
  type: {{.Values.user_service.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.user_service.service.type "NodePort")}}
      nodePort: {{.Values.user_service.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.user_service.service.targetPort}}
      port: {{.Values.user_service.service.port}}
