apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.rating_db.name}}-svc"
spec:
  selector:
    app: {{ .Values.rating_db.name }}
  type: {{.Values.rating_db.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.rating_db.service.type "NodePort")}}
      nodePort: {{.Values.rating_db.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.rating_db.service.targetPort}}
      port: {{.Values.rating_db.service.port}}
