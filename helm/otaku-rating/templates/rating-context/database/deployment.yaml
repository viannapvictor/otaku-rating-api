apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Values.rating_db.name }}-deploy"
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.rating_db.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.rating_db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.rating_db.name }}
    spec:
      containers:
        - name: {{ .Values.rating_db.name }}
          image: "{{ .Values.rating_db.image }}:{{ .Values.rating_db.tag }}"
          imagePullPolicy: {{ .Values.rating_db.imagePullPolicy }}
      restartPolicy: Always
